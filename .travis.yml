language: bash

before_install:
   - sudo apt-get install -y rsync screen

before_script:
    - curl -LSo assert.sh https://raw.githubusercontent.com/Rycieos/assert.sh/master/assert.sh

script:
    - bash tests.sh
